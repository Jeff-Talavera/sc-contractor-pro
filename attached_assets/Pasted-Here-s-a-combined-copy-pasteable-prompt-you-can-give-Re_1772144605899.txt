Here’s a combined, copy‑pasteable prompt you can give Replit’s AI to add BOTH:

Jobsite “Permits / Complaints & Violations” tabs, and

Photo‑upload → AI‑suggested observations flow,
on top of your existing multi‑firm safety app scaffold.

You can lightly edit the bracketed parts (like stack or file names) if needed.

Prompt: Add Public Records (Permits + Complaints/Violations) and Photo‑AI Observation Flow

You are enhancing my existing multi‑organization safety inspection web app for small NYC safety firms. The app already has:

Organizations, users (Owner/Admin/Inspector), clients, and jobsites.

Inspections and observations linked to jobsites.

A code reference library (NYC Chapter 33 + Admin Code metadata, NOT full text).

Now add two major features:

Public records tabs on each jobsite for Permits and Complaints & Violations (with mocked data shaped like NYC Open Data / DOB data).

A photo upload + AI suggestion flow that helps create observations from images, and stores rich metadata for later analytics.

Please use the same tech stack, patterns, and style as the existing project.

1. Extend / confirm data models
If these models already exist, extend them; otherwise, create them in a central types or models file.

a) Jobsite
Ensure Jobsite at least has:

id, organizationId, clientId.

name.

address, borough, bin?, dobJobNumber?.

Flags: hasScaffold, hasHoist, hasCrane, hasExcavation.

New fields:

monitorPublicRecords: boolean (controls whether to watch permits/complaints/violations for this job).

b) Permits (mocked DOB/Open Data shape)
Create a type for permits tied to a jobsite:

ts
type JobsitePermit = {
  id: string;
  jobsiteId: string;
  source: "DOB_NOW" | "BIS" | "NYC_OPEN_DATA";
  permitNumber: string;      // e.g. job/permit number
  jobFilingNumber?: string;
  workType: string;          // e.g. "NB", "ALT-2"
  permitType?: string;       // e.g. "BUILDING", "ELECTRICAL"
  status: "ISSUED" | "EXPIRED" | "REVOKED" | "IN_PROGRESS" | "OTHER";
  issueDate?: string;
  expirationDate?: string;
  description?: string;
  rawLocation?: string;      // address from source
  externalUrl?: string;      // link to DOB NOW/BIS for more detail
  createdAt: string;
  updatedAt: string;
};
Seed a mock dataset for at least 3–4 jobsites to simulate multiple permits per job.

c) Complaints & Violations (mocked DOB/Open Data shape)
Create a generic external event type:

ts
type JobsiteExternalEvent = {
  id: string;
  jobsiteId: string;
  source: "DOB_COMPLAINT" | "DOB_ECB_VIOLATION" | "OTHER";
  eventType: "Complaint" | "Violation";
  externalId: string;          // complaint or violation number
  status: string;              // e.g. "OPEN", "RESOLVED", etc.
  category?: string;           // e.g. "WORK WITHOUT PERMIT"
  description?: string;
  issuedDate?: string;
  lastUpdatedDate?: string;
  rawLocation?: string;
  externalUrl?: string;        // link to BIS, OATH, etc.
  isNew?: boolean;             // for alerting / highlighting
  createdAt: string;
};
Seed mock data for:

At least a couple of complaints and violations on some jobsites.

Some marked isNew: true to show off the UI.

Store all mocked permits and external events in clearly named files (e.g., mockPermits.ts, mockExternalEvents.ts).

d) Observation (extend for photo‑AI and analytics)
Extend the Observation model with:

ts
type AiFinding = {
  id: string;
  label: string;           // e.g. "No guardrails at open edge"
  confidence: number;      // 0–1 or 0–100
  suggestedCodeReferenceIds: string[];
};

type Observation = {
  id: string;
  organizationId: string;
  inspectionId: string;
  jobsiteId: string;
  createdAt: string;
  createdByUserId: string;
  location: string;
  description: string;
  category: string;        // e.g. "Fall Protection", "Housekeeping"
  severity: "Low" | "Medium" | "High";
  status: "Open" | "In progress" | "Corrected" | "Verified";
  assignedTo?: string;
  dueDate?: string;
  photoUrls: string[];     // existing field
  linkedCodeReferenceIds: string[];
  recommendedActions: string[];
  source: "manual" | "ai"; // NEW
  aiFindings?: AiFinding[]; // NEW
};
These fields will support later analytics (counts by category, severity, source, etc.).

2. Jobsite UI: add “Public Records” with tabs
On the Jobsite Detail page, add a Public Records area with tabs:

Permits

Complaints & Violations

Implement them as follows (using the mocked datasets):

a) Permits tab
Read all JobsitePermit entries where jobsiteId matches the current jobsite.

Show a table with columns:

Permit # (clickable to open externalUrl in new tab if present).

Source (DOB NOW, BIS, NYC Open Data).

Work type / Permit type.

Status.

Issue date.

Expiration date.

Add simple filters:

By status (Active vs Expired vs All).

By date range (basic date inputs or quick filters like “Last 6 months”).

If no permits are present, display a helpful empty state message.

b) Complaints & Violations tab
Read all JobsiteExternalEvent entries where jobsiteId matches the current jobsite.

Show a combined table with:

Type (Complaint/Violation).

External ID (clickable link to externalUrl if present).

Category.

Status.

Issued date.

“New” badge if isNew is true.

Allow filtering by:

Type (Complaint vs Violation).

Status.

Date range.

At the top of the tab, show a small summary:

“X open complaints, Y open violations, Z new items since last check.”
(These can be computed from the mock data.)

c) Monitor toggle
In the Jobsite header:

Add a toggle or switch: “Monitor public records for this job (permits, complaints, violations)”.

Bind it to monitorPublicRecords.

For now, this is only UI state; in a later version it will control background polling / alerts.

3. Photo upload → AI‑suggested observations flow
Implement an end‑to‑end UI flow that works with mocked AI responses.

a) Entry point
On the Inspection Detail page and/or Jobsite Detail page, add:

A “Photo Check (AI)” button that opens a dialog or new view.

b) Upload UI
In the Photo Check view:

Show:

Current jobsite and inspection context.

File input / drag‑and‑drop area to upload 1–3 images (for now).

When a photo is selected:

Immediately show a thumbnail preview.

For now, instead of calling a real AI service, call a local mock function that returns 2–4 AiFinding items per image.

Create a function like:

ts
async function mockAnalyzePhotoForHazards(photoUrl: string): Promise<AiFinding[]> {
  // Return hardcoded/simulated findings with labels, confidence, and suggestedCodeReferenceIds.
}
Link suggestedCodeReferenceIds to existing CodeReference entries in the code library.

c) Review AI suggestions
After “analysis” (mock):

Show a list of findings per photo:

Checkbox or toggle to “Create observation from this finding”.

Label + confidence (e.g., “No guardrails at open edge (92%)”).

Chips showing suggested code references (e.g., “BC 3301.2”, “BC 3315.x”).

Provide a text area per finding that pre‑fills a draft observation description using the label, which the user can edit.

d) Create observations from selected findings
When the user clicks “Create observations”:

For each selected finding:

Create a new Observation with:

source: "ai".

description: from the edited text.

category: inferred from label (e.g., if label mentions “guardrail” or “open edge”, set to “Fall Protection”).

severity: maybe default to “Medium” and allow the user to change later.

status: “Open”.

jobsiteId: current jobsite.

inspectionId: current inspection (if in that context), or allow null/standalone.

photoUrls: include the uploaded photo’s URL.

linkedCodeReferenceIds: from suggestedCodeReferenceIds.

recommendedActions: start with 1–2 generic actions derived from category (dummy text is fine for now).

aiFindings: store the original full finding object so we have traceability.

Once created:

Show a confirmation list with links to the new Observations.

On the Inspection Detail page, make sure source is visible in the observations list (e.g., a small “AI” badge vs “Manual”).

e) UX & safety messaging
On the Photo Check view:

Add clear text such as:

“AI suggestions are for guidance only. A qualified safety professional must review and confirm all findings.”

This complements the main app disclaimer.

4. Analytics‑ready design (no charts yet)
We are not building charts yet, but ensure:

Every AI‑generated observation is clearly flagged (source: "ai").

Categories, severity, status, and timestamps are consistently populated.

The JobsitePermit and JobsiteExternalEvent data are easy to query/filter by jobsiteId, type, status, and dates.

This will allow me to later add:

Dashboards like “High‑severity observations per job over last 90 days”.

Counts of new complaints/violations per job.

Ratios of AI‑flagged vs manually logged issues.

5. Developer notes
After implementing these features, please:

Briefly explain:

Which files hold mocked permits and external events.

Which components implement the Jobsite “Public Records” tabs.

Which components/functions implement the Photo Check (AI) flow and where the mock analyzer lives.

Point me to:

Where to replace the mock photo analyzer with a real AI API.

Where to replace mock permits/complaints/violations with real API calls to public NYC datasets.

Keep the code clean and commented enough that a beginner can see where to extend each piece.

